# Written by `flutter gen-l10n` when a translation is behind the template. It is
# a work list, not a source file — the test suite is what actually enforces
# completeness, and a checked-in copy would go stale within a commit.
UNTRANSLATED.txt
